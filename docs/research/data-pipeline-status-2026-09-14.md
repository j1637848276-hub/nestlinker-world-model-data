# 真实采集与历史回填状态（2026-09-14，Asia/Seoul）

## 真实持续采集

- 完整正式版本：1 次。2026-09-07 批次 `20260907T115721Z`，900/900 分区完整，448,077 条原始及标准化记录，审核通过，未允许公开明细发布。
- 本周尝试：`20260913T211803Z`（UTC；首尔日期为 9 月 14 日），计划 900 分区。
  78 个已尝试分区均 HTTP 403，停止进程以避免继续无效请求；保留中断批次，没有补造完整状态。
- 独立权限复查：本周 3 来源 × 2 月的 6 个分区均失败，0 个完整；测试存储 `data/raw/rtms-smoke` 不算正式观察。
- 可见正式尝试 2，完整版本 1，中断批次 1；全体计划分区完整率 900/1800=50%。
- 两个完整成功版本之间的实际观察跨度仍为 0 天，可靠版本比较 0 次；不能估计真实迟报、修订、消失或时效分布。
  `estimated_missed_scheduled_runs=0` 仅描述完整版本之间的间隔，不能推断尾部没有失败或漏跑。
- 当前 Key 存在于 Windows 用户环境变量；没有复制进仓库或报告。403 原因尚未确定，需检查门户批准/暂停/到期状态以及接口访问环境。
- 本地定时任务已启用：每周一 03:00，下一次 2026-09-21；每月 1 日 04:00，下一次 2026-10-01，首尔时间。
  每次先执行小范围权限复查，失败即停止主采集并告警。
- 存储为本机 `data/raw/rtms-observations`，不是临时 CI artifact；原始记录与实际配置都被 Git 忽略。
  尚缺持续在线主机保证、独立备份和云端受控存储。跨电脑 checkout 不会自动拥有原始版本或凭据。

本地计划需要电脑开机且桌面应用运行，不是关机后仍执行的云端托管服务。
这一部署限制按 OpenAI Docs 核对：[官方定时任务说明](https://learn.chatgpt.com/docs/automations?surface=app)。

## 2011–2024 历史审核与发布

- 使用真实 2026-09-05 下载批次，14 个 ZIP，6,139,058 条源行；没有伪造过去获取时间。
- 9 月 14 日重跑全部年度质量审核，与台账哈希和字节数绑定；14 年完整，均 `needs_review`，不是宣称源文件无问题。
- 本轮明确采用既有同年度价格视图处理审核问题：合同年=受理年=文件年，字段有效，发布单元至少 10 行。
  迟报保留在原始 ZIP，但不进入这一保守回放视图；没有重建完整合同历史或任意历史日的 as-of 数据。
- 2024 的一个额外精确重复保留来源出现次数，不擅自判断唯一交易；2025 仍整体隔离。
- 新快照：`data/snapshots/2026-09-14/seoul-rental-history`，32,085 个聚合组，代表 5,569,590 条来源行。
- 旧历史快照：2022–2024、7,125 个聚合组，代表 1,586,723 条来源行，未改动。
  新旧快照重叠，不应相加当成独立合同数。

数据守恒（互斥去向）：

| 去向 | 来源行数 |
| --- | ---: |
| 合同年不同于文件年 | 554,649 |
| 受理年不同于合同年 | 6,424 |
| 当前聚合规则无效行 | 11 |
| 小样本组抑制 | 8,384 |
| 公开聚合所代表来源行 | 5,569,590 |
| 总计 | 6,139,058 |

审核问题计数可重叠，不能替代这张互斥守恒表。处理规则见 `docs/SEOUL_HISTORY_BACKFILL.md`。

## 工程交付

- 分支：`codex/backfill-seoul-history`，基于持续采集分支最新提交。
- 新增 TXT/CSV 编码和分隔符适配、file_year 台账读取、哈希绑定审核门禁、逐年行数核对及发布守恒检查。
- 修复 Windows 调度进程凭据导入，以及中断版本审计的计划分区分母和汇总可见性。
- 本地 65 个 unittest 通过；`python -m worldmodel_data validate` 通过（34 来源、3 快照）。
- 只公开代码、聚合快照与聚合说明；不上传原始合同明细、密钥或细分审核报告，不训练大模型或安全评分。
