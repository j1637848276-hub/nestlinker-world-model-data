# RTMS 连续版本采集运行状态（2026-09-07）

## 当前真实状态

- 管道代码：已实现，等待代码审查与部署。
- 真实成功采集次数：0。
- 实际连续观察时间：0 天 / 0 个月。
- 下一次真实计划运行时间：尚未设置。
- 已启用生产调度：否。
- 已配置 `DATA_GO_KR_SERVICE_KEY`：当前开发进程中没有。
- 已配置长期持久存储：尚未指定生产路径；本地默认目录被 Git 忽略。
- 已生成真实迟报、修订或时效结论：否。

当前报告不能宣称已开始三个月观察期。观察期从第一批真实、完整且带时间戳的运行开始计算；查询过去 12 个合同月份不增加实际观察时长。

## 已实现

- 每周滚动 12 个月、固定月份队列和可选低频补查配置；
- 不可覆盖 run_id、未完整运行恢复和并发互斥；
- 来源×地区×合同月份及逐页完整性记录；
- 有上限的重试、退避、限流和错误密钥脱敏；
- 内容寻址原始响应与年度 ZIP 版本记录；
- 完整字段指纹的多重集合版本比较；
- baseline_existing、first_seen 区间和真实观察跨度区分；
- 采集、完整、审计和公开准入四种状态；
- 聚合质量报告、PowerShell 调度入口和 systemd 示例；
- 两个版本的合成端到端演示及自动测试。

## 启动真实采集前缺少的配置

1. 有权限的公共数据门户 decoding key；
2. 生产配置文件及固定合同月份队列；
3. 持续在线的运行主机或受控作业平台；
4. 有容量规划、备份和访问控制的持久存储路径；
5. 非零退出码告警接收方式；
6. 对运行频率、API 配额和来源条款的上线复核。

只有完成这些配置并出现第一批真实完整运行后，才能填写下一次计划时间和开始计算三至六个月观察期。

## 本地验收证据

- `python -m unittest discover -s tests -q`：62 个测试通过；
- `python -m worldmodel_data validate`：34 个来源、2 个既有快照通过验证；
- Python 编译、配置 JSON、PowerShell 脚本解析及 Git 空白检查通过；
- 合成端到端演示：2 个 synthetic 完整运行、1 次可靠比较、1 个新增内容出现次数；
- 合成演示报告中的真实运行数为 0、真实观察跨度为 0，未混入真实覆盖统计。
