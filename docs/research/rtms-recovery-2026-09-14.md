# RTMS 真实采集恢复报告（2026-09-14 晚间，Asia/Seoul）

## 已实际完成

最新正式批次 `20260914T095742Z` 已结束：开始于 UTC 09:57:42，结束于 UTC 10:04:32，即首尔 18:57–19:04。首尔 25 个行政区、3 种住宅来源、12 个合同月份，共 900/900 分区完整，取得 457,590 条原始及标准化记录。采集成功、版本完整、标准化审计通过；允许公开发布明细仍为 false。

首次正式完整批次为 `20260907T115721Z`，448,077 条记录。本轮实际成功正式采集共 2 次，不包含独立权限测试。观察起点 UTC 2026-09-07 11:57:21，最新观测 UTC 2026-09-14 09:57:42，跨度约 6 天 22 小时；审计的完整天数为 6、完整自然月数为 0。查询过去 12 个合同月份不等于拥有 12 个月观察历史，连续 3 个月与目标 6 个月均未满足。

## 失败原因与恢复

此前安全诊断取得来源错误 `SERVICE_KEY_IS_NOT_REGISTERED_ERROR`、原因码 30：门户不认可当时使用的服务密钥，不是合法零条数据。用户重新签发后，小范围权限验证成功，6/6 分区、592 条记录。

另一次正式入口仍继承旧进程环境中的密钥，因此继续失败。已修复 Windows 入口：存在当前用户级凭据时，优先于旧进程凭据；没有用户级凭据时仍允许进程环境配置。新密钥仅存本机环境变量，临时 `key.txt` 已删除，未写入代码或报告。

两次中断正式批次 `20260913T211803Z`、`20260914T095634Z` 保留原状，其 manifest 的 running 表示未正常结束，不代表进程仍在执行；均未计为完整成功或用于可靠版本差异。可见正式尝试 4 次，完整成功率 2/4=50%；计划分区完整率 1,800/3,600=50%。不能只展示恢复后的成功而隐去失败。

## 两个完整版本的描述性比较

查询范围和 schema 一致，900 个分区可可靠比较。按完整原始字段指纹及出现次数进行多重集合比较，而不是把内部生成 ID 当官方稳定交易 ID。

| 指标 | 结果 |
| --- | ---: |
| 新增内容出现次数 | 9,554 |
| 消失内容出现次数 | 41 |
| 净出现次数变化 | +9,513 |
| 疑似修订候选计数上界 | 37 |
| 聚合内容变化率 | 2.096837% |

变化率分子是新增与消失出现次数之和 9,595，分母为每个可比较分区两次记录数较大值的总和。疑似修订候选计数取每个分区新增、消失次数较小值再求和，不是交易配对或已确认修订。消失不代表撤销或删除；新增也不能全部归为迟报。没有官方稳定交易 ID，不能确认同一交易的修订。

两个完整版本累计 905,667 条记录观测，精确重复的额外出现次数为 24,099，分母为这些记录观测；不据此擅自删除交易。当前审计所检查的标准化字段没有缺失计数，不代表所有原始字段齐全。明确来源申报时间的指纹计数为 0，不能估计官方首次发布日期或真实申报延迟。首轮已有记录仅作 baseline，不参与发现延迟估计；后续发现时间受前次完整观测与本次观测的时间区间约束。

本地详细审计位于 `data/work/rtms-version-audit/2026-09-14-recovered`，含不可公开的内容指纹，不提交 Git。本报告只公开汇总。

## 下一次计划与尚缺条件

已配置本地每周一 03:00 采集，下一次计划为首尔时间 2026-09-21 03:00；低频补查下一次为 2026-10-01 04:00。计划时间不等于承诺执行成功。实际配置、原始响应、版本和审计保存在本机 Git 忽略目录，持续存储不依赖 CI artifact。

尚缺持续在线主机保证、独立备份及云端受控持久化存储。本地计划要求电脑开机且桌面应用运行；其他电脑拉取 Git 后仍需安全配置自身凭据和存储，不能自动继承真实历史版本。历史 2011–2024 聚合回填已单独发布，见当日早间管道报告；它不增加连续版本观察时长。

## 验证与交付边界

本地 65 个 unittest 通过，`python -m worldmodel_data validate` 通过（34 来源、3 快照）；凭据入口修复提交 `a78517e` 的 GitHub CI 已成功。正式成功批次记录采集时的代码提交 `4dccfd6`，未伪改为后续修复提交。

已实现不可覆盖版本采集、分页完整性、重试与断点恢复、版本比较和描述性审计，以及 Windows 最新用户凭据优先修复。本次不训练大模型、不生成安全评分、不修改既有已发布历史快照，不上传密钥或原始合同明细。
