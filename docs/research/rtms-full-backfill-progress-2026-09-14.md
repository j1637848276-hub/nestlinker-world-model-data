# 全历史 API 回填进展（2026-09-14，首尔 20:48 更新）

这是进行中的进度报告，不是全历史采集完成报告。

本次范围沿用首尔 25 区、已批准的 apartment / officetel / single_multi 三个接口，历史计划 2011-01 至 2025-09，15 年配置、13,275 个来源×地区×月份分区；近期 2025-10 至 2026-09 已有独立完整版本。未自动扩展全国、第四种住宅接口或计划外年份，不宣称这些日期是 API 全历史边界。

已完成 2025-01 至 2025-09 基线 392,295 条、675/675 分区。全历史控制器 `20260914T114457225917Z` 于 UTC 11:44:57（首尔 20:44:57）启动，真实提交 `ca62591`、工作区 clean。已跳过这一完整基线，正在 2024 年采集，后续按 2023 至 2011 顺序接续。2024 及后续年份尚未写下合格完成结果，因此本报告不把部分记录加入成功版本总数。

控制器状态保存在本机 `data/raw/rtms-history-observations/control/executions`，逐年存储位于 `by-year/<year>`；完成年份自动生成独立审计。每个 API 本次执行预算 6,000 请求尝试，错误/预算/质量不通过即停止，并保留原始响应、已完整分区和真实时间。此预算不是官方日额度剩余量，不能反复重启绕过。

已新增本机明细导出，逐页对象哈希、分区和总行数绑定，UTF-8/LF，固定导出源码哈希与输入 manifest。独栋/多户面积保留 `source_totalFloorAr` 口径，不假装专有面积；first_seen 未编译的值保留 null，不伪造发布日期。近期 457,590 条与历史基线 392,295 条正在独立整理，只有生成完整导出 manifest 后才算成功。不是自动公开聚合发布。

全历史初次采集尚未完成，历史低频计划当前仍仅覆盖 2025-01 至 2025-09；下一次 2026-10-01 04:00。近期周度下一次 2026-09-21 03:00。更早年份低频计划、跨来源年度口径对照与公开 API 聚合发布尚待后续处理。观察自然月数仍未达 3，不将历史合同年份算作连续观测时间。

控制器与导出代码位于分支 `codex/rtms-history-observation`；本地 72 个 unittest 和 validate（34 来源、3 快照）通过。控制器与导出提交 `0ab1320` 的 Windows/Linux CI 均已通过。只上传代码和汇总报告，原始交易明细、实际配置、操作清单、密钥均留在 Git 忽略目录。

后续核验：2025 基线明细已生成完整 manifest，392,295 条，SHA-256 和总数绑定完成。可选字段缺失：合同期限 13,484；专有面积与楼层各 116,590（独栋/多户来源并不提供同口径字段）；来源明确申报时间 392,295。这些缺失不转成零，也不把可选字段缺失误称为采集分页失败。

仍需本机在线、持久目录和备份；没有已配置的云端受控存储或完整独立备份，不保证电脑关机后继续运行。
