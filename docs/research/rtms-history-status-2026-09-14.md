# 两条 API 采集线状态（2026-09-14，Asia/Seoul）

## 近期每周线

保持既有近期滚动 12 月加固定研究月份队列，每周一 03:00 本地计划，下一次 2026-09-21。正式完整版本 2 次，最新 457,590 条记录；真实观测约 6 天 22 小时，完整自然月为 0。记录新增与消失，不自动确认修订或真实迟报，详见当日晚间恢复报告。

## 历史首次回填

已经生成 2011-01 至 2025-09 的逐年配置，只有 2025-01 至 2025-09 已实际执行。批次 `20260914T102730Z`，UTC 10:27:30 至 10:32:59，即首尔 19:27–19:32；真实代码提交 `aded11a`，工作区 clean。

| 来源 | 实际记录数 |
| --- | ---: |
| 公寓 | 206,270 |
| 办公住宅 | 69,435 |
| 独栋／多户 | 116,590 |
| 合计 | 392,295 |

25 区 × 3 来源 × 9 月，共 675/675 分区完整，0 个不完整分区；原始与标准化均 392,295 条。采集成功、版本完整、标准化审计通过，公开明细发布资格仍为 false。记录数保留来源出现次数，不等于确认的唯一合同数。

历史线独立目录 `data/raw/rtms-history-observations`，配置位于 `data/work/rtms-history-plan/2026-09-14`，审计位于 `data/work/rtms-history-audit/2026-09-14-first`，均不提交 Git。历史线真实完整运行 1 次，观察跨度 0 天，可靠版本比较 0 次；首轮均为 baseline，未伪造过去观测时间。2011–2024 API 尚未完整回填，不能把已发布年度 ZIP 聚合快照算作 API 回填完成。

## 已部署与尚缺

既有每月低频计划已更新为历史独立配置，当前仅复查 2025-01 至 2025-09，下一次首尔 2026-10-01 04:00。每次先做独立权限预检，失败不启动主批次。更早年份的首次回填与低频计划仍待分批推进及配额核对；没有自动启动全历史请求。

本地磁盘和用户环境凭据可用；尚缺持续在线主机保证、独立备份与云端受控持久化存储。本地计划要求电脑开机且应用运行，按 [OpenAI Docs 官方说明](https://learn.chatgpt.com/docs/automations?surface=app) 核对，不能承诺计划一定成功执行。

API 与年度文件不互相覆盖，不直接相加。独立口径对照、API 聚合发布与住宅面积字段适配仍是后续开发，不因采集成功自动入公开快照。本次未训练模型、未生成安全评分、未修改已发布历史快照。

## 工程验证

67 个 unittest 通过，`python -m worldmodel_data validate` 通过（34 来源、3 快照）。新增历史-only 配置测试、逐年分批与不可覆盖计划测试；无新增依赖，Python >=3.9。功能提交 `aded11a` 的 Windows/Linux GitHub CI 成功。源码与本报告位于分支 `codex/rtms-history-observation`，原始数据和密钥未上传。
